# CLAUDE.md — 사진 베스트컷 선별 앱

이 파일은 클로드 코드가 이 프로젝트에서 작업할 때 참고하는 가이드입니다. 작업을 시작하기 전에 이 문서와 PROJECT_SPEC.md를 함께 읽고, 스펙에 없는 애매한 부분이 있으면 임의로 추측해 구현하지 말고 먼저 질문하세요.

## 프로젝트 한 줄 요약

지정한 폴더 안의 사진 전체를 스캔해서, 같은 장면(동일 순간을 연속 촬영한 사진들)끼리 자동으로 묶고, 각 장면에서 화질/구도/표정 등을 기준으로 가장 좋은 컷을 골라 "보정 진행 예정" 폴더로 복사(또는 이동)해주는 도구.

## 개발 원칙

- 원본 보호가 최우선. 어떤 단계에서도 원본 파일을 삭제하는 코드는 작성하지 않는다. 기본 동작은 복사(copy)이며, 이동(move) 기능은 별도 옵션으로만 제공한다.
- 작게 만들고, 매번 검증한다. 폴더 스캔 → 장면 그룹핑 → 품질 스코어링 → 파일 이동 순서로 단계마다 독립적으로 동작 확인 후 다음 단계로 넘어간다. 한 번에 전체 파이프라인을 구현하지 않는다.
- 모든 실행은 dry-run이 기본값. 실제로 파일을 복사/이동하기 전에 "이 사진을 왜 선택/제외했는지"를 로그나 리포트로 먼저 보여준다. `--apply` 같은 명시적 플래그가 있을 때만 실제 파일 작업을 수행한다.
- 테스트 폴더 기준으로 회귀 테스트. `test_photos/` 샘플 폴더(다양한 장면, 다양한 품질의 사진 모음)를 기준으로 결과가 항상 일정 수준 이상 유지되는지 확인한다. 로직을 수정한 뒤에는 이 샘플로 결과가 나빠지지 않았는지 비교한다.
- 로그를 남긴다. 어떤 사진이 어떤 장면 그룹에 속했는지, 각 사진의 점수가 몇 점이었는지, 왜 특정 컷이 선택됐는지를 사람이 읽을 수 있는 형태(CSV/JSON/로그 파일)로 남긴다.
- UI는 로직이 안정된 후에. CLI로 로직을 충분히 검증하기 전까지는 GUI 작업에 시간을 쓰지 않는다.

## 기술 스택 (기본값, PROJECT_SPEC.md에서 확정)

- 언어: Python
- 이미지 처리: Pillow, OpenCV
- EXIF 추출: exifread 또는 piexif
- 유사도/클러스터링: imagehash, scikit-learn
- 얼굴/표정 검출: mediapipe
- CLI 우선 구현, 이후 필요 시 PyQt / Streamlit 중 택1로 GUI 확장

## 작업 순서 (마일스톤)

- [x] M1: 폴더 스캔 + EXIF 메타데이터 추출 (지원 확장자, 하위 폴더 옵션 포함)
- [x] M2: 장면 그룹핑 (촬영 시각 간격 기준 1차 구현 → 필요 시 이미지 유사도 보강)
- [x] M3: 품질 스코어링 (선명도 → 노출 → 얼굴/눈감음 순으로 단계적 추가) — 3개 지표 구현 완료
- [x] M4: 장면별 베스트 컷 선정 로직 + 동점 처리 — selector.py, 가중치 초기값 sharpness 0.5/exposure 0.3/face 0.2 (test_photos 결과로 추후 조정 가능)
- [x] M5: dry-run 리포트 출력 (선택/제외 이유 포함)
- [x] M6: 실제 파일 복사/이동 + 로그 기록
- [x] M7: 회귀 테스트 세트 구축 (test_photos 기준) — 실사진 22장(JPG+CR3)으로 기준선 확정. 얼굴 인식은 1/22장만 성공(측면/대화 컷이 많아 정면 검출기가 잘 못 잡음, 버그 아님 — 결과적으로 현재 세트에서는 사실상 선명도+노출 위주 선정). 로직 바꿀 때마다 `pytest`가 자동 회귀 감지
- [x] M8: GUI 래핑 (PySide6, FrameSense.app로 패키징 완료) — 1차 범위: 폴더 선택/미리보기/적용/진행률. 장면별 이미지 미리보기·수동 변경은 다음 버전
- [x] M9: 시간순 정렬 + 00001부터 순번 리네임 — 여러 바디 폴더를 동시에 입력받아 EXIF 촬영 시각으로 합쳐 정렬, JPG+RAW를 같은 번호로 짝지어 제자리(in-place) 리네임. 원본은 옮기지 않고 각자 폴더에서 파일명만 바뀜(원본 보호 원칙에 따라 새 폴더 복사 대신 사용자가 명시적으로 선택한 방식). 배치 전체를 임시 이름으로 비운 뒤 최종 이름을 부여하는 2단계 방식으로 이름 겹침(스왑)에도 데이터 손실 없음. 영상 파일(mp4/mov/avi/mts/m4v)도 이 기능에서만 함께 정렬·리네임 대상 — EXIF가 없어 파일 mtime을 촬영 시각으로 쓰고, RAW 짝짓기 대상에서는 제외(베스트컷/C컷의 화질 점수는 사진 전용이라 영상엔 적용 안 함) — `src/rename_by_time.py`, GUI "시간순 리네임" 탭, CLI `rename` 서브커맨드
  - "한 폴더로 합치기" 옵션(체크박스/`--merge-output`) 추가: 여러 바디 폴더 대신 지정한 출력 폴더 하나로 모아 이동(원본 바디 폴더에서는 빠짐), RAW는 `output/RAW/`에 모임 — `build_merge_plan`/`apply_merge_plan`
  - `find_matching_raw`가 같은 폴더에 없으면 `RAW/` 서브폴더도 확인하도록 확장 — `보정 진행 예정/RAW/` 레이아웃(베스트컷 복사본)도 이 기능으로 시간순 리네임 가능
- [x] M10: C컷(흔들림/눈감음) 이동 — select_best와 달리 장면 그룹과 무관하게 스캔된 사진 전체를 절대 임계값(선명도·눈뜬 정도)으로 판정. 임계값은 코드에 기본값을 두지 않고 매번 사용자가 지정(스펙에 없는 판단 기준을 임의로 확정하지 않는다는 원칙). 이동은 기존 `file_ops.apply_selection(move=True)`를 그대로 재사용(JPG+RAW 짝짓기·로그 포함) — `src/badcut.py`, GUI "C컷 정리" 탭, CLI `badcut` 서브커맨드

각 마일스톤은 별도 커밋/PR 단위로 나누고, 완료 시 이 체크박스를 갱신한다.

## 하지 말아야 할 것

- 원본 파일 삭제 또는 덮어쓰기
- 사용자 확인/리포트 없이 대량 파일을 곧바로 이동하는 기능을 기본값으로 두는 것
- 스펙에 명시되지 않은 임의의 "장면" 판단 기준이나 점수 가중치를 마음대로 확정하는 것 (PROJECT_SPEC.md의 "결정 필요 항목" 참고, 애매하면 사용자에게 확인)

## 참고 문서

- PROJECT_SPEC.md — 상세 요구사항, 아키텍처, 폴더 구조, 결정 필요 항목

## Agent skills

### Issue tracker

GitHub Issues (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.
