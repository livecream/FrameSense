# 프로젝트 스펙 — 사진 베스트컷 자동 선별 & 정리 앱

## 1. 개요

지정한 폴더 안의 사진들을 스캔하여, 같은 장면(동일 순간을 연속 촬영한 사진 묶음)을 자동으로 인식하고, 각 장면에서 화질/구도/표정 등의 기준으로 가장 좋은 컷을 선정한 뒤, "보정 진행 예정" 폴더로 복사(또는 이동)하는 도구. 최종 목표는 사람이 수동으로 사진을 한 장씩 넘기며 고르는 작업(컬링, culling)을 자동화하는 것.

## 2. 목표 / 비목표

### 목표

- 대량의 사진 중 장면별 베스트 컷을 자동으로 골라 후속 보정 작업량을 줄인다.
- 원본 라이브러리를 훼손하지 않는다.
- 선택 결과와 이유를 사람이 검토할 수 있게 한다.

### 비목표 (1차 버전 범위 밖)

- 보정(리터칭) 자체는 하지 않는다. 어디까지나 "베스트 컷 선정 및 분류"까지.
- 클라우드 동기화나 다중 사용자 협업 기능은 포함하지 않는다.
- RAW 파일의 완전한 편집 지원(1차는 미리보기/메타데이터 읽기 수준으로 충분).

## 3. 입력 / 출력

- 입력: 사용자가 지정한 폴더 경로 (하위 폴더 포함 여부는 옵션). 지원 포맷: JPEG, PNG, HEIC, (선택) RAW(CR2/NEF/ARW 등 — 1차는 후순위).
- 출력:
  - `보정 진행 예정/` 폴더에 장면별 베스트 컷 복사(기본) 또는 이동(옵션).
  - `보정 진행 예정/RAW/` 하위 폴더: 선정된 베스트 컷과 동일한 베이스 파일명의 RAW 파일이 원본 폴더에 있으면 함께 복사.
  - 처리 리포트 (CSV 또는 JSON): 원본 경로, 소속 장면 그룹, 점수, 선택 여부, 제외 사유.
  - 제외된 사진은 별도로 옮기지 않고 원본 위치에 그대로 둔다.

## 4. 파이프라인 아키텍처

```
[폴더 스캔] → [메타데이터 추출] → [장면 그룹핑] → [컷별 품질 스코어링]
  → [장면별 베스트 컷 선정] → [리포트 생성] → [파일 복사/이동]
```

### 4.1 폴더 스캔 & 메타데이터 추출

- 지원 확장자 필터링, 하위 폴더 재귀 탐색 여부 옵션.
- EXIF에서 촬영 시각(DateTimeOriginal), 카메라 설정(조리개/ISO/셔터스피드), 방향(orientation) 추출.
- EXIF가 없는 파일(스크린샷 등)의 처리 방식 정의 필요.

### 4.2 장면 그룹핑

- 1차: 촬영 시각 간격 기준 (예: N초 이내 연속 촬영 시 같은 장면으로 묶음. N은 설정 가능한 값).
- 2차(보강): perceptual hash(이미지 유사도)로 시간 간격만으로는 부정확한 경우 보완.
- 그룹 최소/최대 크기 정책 필요 (예: 단독 사진 1장도 하나의 "장면"으로 취급할지).

### 4.3 품질 스코어링

각 사진에 대해 아래 항목별 점수를 계산하고 가중합으로 종합 점수 산출.

| 항목 | 방법 | 우선순위 |
|---|---|---|
| 선명도(블러 여부) | 라플라시안 분산 등 | 1차 필수 |
| 노출 적정성 | 히스토그램 분석 | 1차 필수 |
| 얼굴/눈감음/표정 | mediapipe 등 얼굴 검출 모델 | 인물 사진에서 필수, 풍경 사진에는 미적용 |
| 구도/미학 점수 | (선택) 사전학습 미학 평가 모델 | 2차 고도화 항목 |

가중치는 초기값을 정하고, 실제 테스트 폴더 결과를 사람이 검토하며 조정.

### 4.4 베스트 컷 선정

- 장면 그룹 내 최고 점수 사진 1장 선정 (동점 시 처리 규칙 필요).
- (선택) 장면당 1장이 아니라 상위 N장을 남기는 옵션 제공 여부 결정.

### 4.5 리포트 & 파일 처리

- dry-run 모드: 실제 파일 작업 없이 리포트만 생성.
- apply 모드: 리포트 검토 후 명시적으로 실행 시 실제 복사/이동 수행.
- 모든 처리 내역은 타임스탬프가 포함된 로그 파일로 저장.

## 5. 폴더 구조 (제안)

```
photo-culling-app/
├── CLAUDE.md
├── PROJECT_SPEC.md
├── src/
│   ├── scanner.py       # 폴더 스캔 + EXIF 추출
│   ├── grouping.py      # 장면 그룹핑
│   ├── scoring.py       # 품질 스코어링
│   ├── selector.py      # 베스트 컷 선정
│   ├── file_ops.py      # 복사/이동 + 로그
│   └── cli.py           # CLI 진입점
├── tests/
│   └── test_*.py
├── test_photos/         # 회귀 테스트용 샘플 사진 (다양한 장면/품질)
└── output/              # 실행 결과 (리포트, 로그)
```

## 6. 사용자 인터페이스

- 1차: CLI. 예) `python cli.py --input ~/Photos/2026-09 --output ~/Photos/보정대기 --dry-run`
- 2차: GUI (로직 검증 후 착수). 폴더 선택, 진행률 표시, 장면별 결과 미리보기(선택된 컷과 제외된 컷을 나란히 보여주고 수동으로 바꿀 수 있는 기능) 정도의 범위로 시작. 프레임워크는 PyQt(네이티브 느낌) 또는 Streamlit(빠른 개발) 중 택1.

## 7. 안전장치

- 원본 삭제 코드 없음. 기본 동작은 복사.
- 이동(move) 옵션은 사용자가 명시적으로 활성화했을 때만 동작.
- 파일명 충돌 시 덮어쓰지 않고 자동으로 넘버링.
- 처리 전/후 파일 개수 검증 (복사 누락 감지).

## 8. 결정된 항목 (사용자 확인 완료, 2026-09-16)

- **장면 그룹핑 간격**: 기본값 3초 (결혼 스냅 특성 반영). `--scene-gap-seconds` CLI 옵션으로 조절 가능.
- **평가 방식**: 인물/풍경 구분 없이 통합 점수 하나로 처리 (1차).
- **장면당 선정 수**: 1장 고정.
- **원본 처리**: 기본 복사(copy), 이동(move)은 명시적 옵션에서만 (기존 결정 유지).
- **제외 사진 처리**: 별도 폴더로 옮기지 않고 원본 위치에 그대로 둔다. 리포트에만 제외 사유 기록.
- **RAW 파일 처리**: RAW(CR2/NEF/ARW 등)는 스캔·스코어링 대상에서 제외한다. 다만 베스트 컷으로 선정된 파일과 동일한 베이스 파일명의 RAW 파일이 존재하면, 그 RAW 파일도 함께 출력 폴더로 복사한다. 출력 폴더 내에는 RAW 전용 하위 폴더(`RAW/`)를 두어 따로 모은다.
- **처리 규모**: 회당 수백~수천 장 수준을 기준으로 설계 (스트리밍/배치 처리까지는 불필요).
- **UI 로드맵**: 1차는 CLI로 로직을 충분히 검증(M1~M7)한 뒤, 이후 macOS 대상 GUI(M8)를 진행.

## 9. 마일스톤 (CLAUDE.md와 동일, 진행 상황 추적용)

- [x] M1: 폴더 스캔 + EXIF 추출
- [x] M2: 장면 그룹핑
- [x] M3: 품질 스코어링
- [x] M4: 베스트 컷 선정 로직
- [x] M5: dry-run 리포트
- [x] M6: 실제 파일 복사/이동 + 로그
- [x] M7: 회귀 테스트 세트 구축
- [x] M8: GUI 래핑

## 10. 성공 기준

- 테스트 폴더에서 사람이 수동으로 골랐을 때와 베스트 컷 선정 결과가 80% 이상 일치.
- 원본 파일 손상/삭제 사고 0건.
- 처리 사유(리포트)를 보고 왜 이 컷이 선택됐는지 비전문가도 이해 가능.
